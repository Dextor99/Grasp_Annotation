1.gripper_model：建立了夹爪模型，单个夹爪模型，可调整参数：开口大小、厚度、宽度、高度等
2.gripper_demo：存储夹爪集合，调整旋转角、开口度，生成类圆柱表达
3.cloud_process：针对输入点云，生成球面视角
4.cloud_cylinder_sections：针对单个视角，生成首次接触到物体点云的圆柱面，以及延申100mm后的圆柱面，表示夹爪深度100mm
41.cloud_cylinder_sections2：针对单个视角，生成圆柱截面中心首次接触到物体点云的圆柱面，以及延申100mm后的圆柱面，表示夹爪深度100mm
5.gripper_axis：将生成的夹爪集合，通过坐标变换，使夹爪Z轴和生成的圆柱面中心点法线重合，夹爪坐标系原点和中心点重合，生成该视角下的夹爪接近方向
6.gripper_move：夹爪以d间隔从t0移动到t1，显示功能集成在主程序中
7.gripper_move2：夹爪移动结果，最后返回了每个移动点夹爪集合中夹爪参数
8.gripper_collision_filter:t0面是圆柱截面所在平面首次接触物体点云的面。加入碰撞检测，对每移动一次得到的夹爪姿态集合进行碰撞检测，保留和物体点云无碰撞的姿态
81.gripper_collision_filter2:t0面是圆柱截面中心首次接触物体点云的面。加入碰撞检测，对每移动一次得到的夹爪姿态集合进行碰撞检测，保留和物体点云无碰撞的姿态

estimate_normals：估计物体法线
grasp_detect_V0：发现夹爪坐标系没有改变，模型是对的，但是夹爪坐标系原点没有正确变换，导致在OBB检测时，产生误检，OBB构建位置错误
grasp_detect_V2：开始解决上面提到的问题，一步一步查原因，逐步把筛选出来的夹爪列表里面的夹爪坐标系原点显示出来，发现原点不在中间，一步一步查坐标转换关系
grasp_detect_V3：统一了涉及到的所有坐标系名字写法，修改了里面存在的坐标系转换问题，解决上述问题
grasp_detect_V4：在夹爪列表中加入内部点等序列集合，便于后续操作
grasp_detect：grasp_detect：基于后续的评分指标计算，加入了内部点云法线、物体在世界坐标系下的表示等标签


grasp_score:实现了对前6项指标的求解
grasp_score_V2:加入了夹爪内部点云的法线估计，计算极值点和夹爪内壁的夹角
grasp_score_V3:尝试加入力闭合评分


ply_path = "model/huixing.ply"
    i=0
    candidate_grippers,candidate_grippers_meshes,vis_list = grasp_detect(ply_path,i)
1.夹爪内点云数占总点云比例：计算夹爪内部点，占物体点云的比离
2.内部点云z值最小值（夹爪坐标系），
3.内部点云z值最大值（夹爪坐标系），
4.内部点云在夹爪平面的投影。将夹爪内点云，投影在夹爪坐标系下的xoz平面，计算投影点凸包的面积；
5.内部点云在夹爪平面的投影。将夹爪内点云，投影在夹爪坐标系下的xoz平面，计算投影点凸包的面积，然后计算凸包面积与夹爪指面面积的比值
6.离两夹爪最近点的差值：就是在夹爪坐标系下找夹爪内点云中y值最小和最大的点，如果极值点不是一个，那就用第一个找到的极值点；或者在极值点中随机选一个
7.内部点云离夹爪两指最近点的法向量与夹爪指面夹角：上一步找到两个极值点后，算两个点法线方向，计算y极大值点法线与y轴正向指面法线（0，-1，0）的夹角；计算y极小值点法线与y轴负向指面法线（0，1，0）的夹角，都在0-180度
8.计算第七步中求到的两个夹角差值的绝对值。

[86.09541823398118, 86.62922027450044, 65.34437185753715, 84.76785084657885, 145.8150691862463, 147.3785558202455,
126.52845218925366, 146.38255638052667, 209.33723058196915, 209.69236516586005, 189.7894186447669, 208.53731121292435]

for i, g in enumerate(candidate_grippers):
    score = g.get('score_force_closure', None)
    print(f"Gripper #{i} score_force_closure: {score}")


性能分析
--------

当前最终入口为 `grasp_score_calculate.py`。性能计时默认关闭；在项目根目录执行：

```powershell
$env:GRASP_PROFILE = "1"
F:\Miniconda\envs\py310\python.exe .\grasp_score_calculate.py
```

程序结束后会输出各阶段秒数和占比，包含点云读取、体素下采样、法线估计、KD 树、frame 生成、夹爪候选生成、沿 Z 轴移动、碰撞筛选、评分、Top-K 和可视化。计时由 `profiling.py` 提供，不开启环境变量时不会改变原有输出。

同时会输出候选数量：夹爪旋转/开口组合数、进入碰撞筛选数、碰撞通过数、开口筛选数和最终点云相交数。当前实现的单方向候选来自一个选定 frame：132 个旋转/开口组合乘以 16 个深度层，共 2112 个候选，并不是 500 个 frame 的遍历。


